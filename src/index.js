import { styleBody, addTitle } from './dom';

const greet = name => {
    let nam = name;
    alert(`Hey ${nam}`);
};

greet('I am coming from the index.js');

// addTitle('hellooo');
// styleBody();
